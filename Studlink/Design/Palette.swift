import SwiftUI

/// Named colours from the asset catalog. Views never read hex.
enum Palette {
    static let background = Color("sdl_background")
    static let surface = Color("sdl_surface")
    static let ink = Color("sdl_ink")
    static let accent = Color("sdl_accent")
    static let muted = Color("sdl_muted")
}
